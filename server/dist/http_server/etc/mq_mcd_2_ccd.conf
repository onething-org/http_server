<root>
		 shm_key = 1001925940
		 shm_size = 134217728
#		 sem_key = 287454020
#		 sem_index = 1
#		 sem_size = 8
		 #dequeue的时候是否加锁，当只有一个进程dequeue时可以不加，否则需要加锁
		 sem_rlock = 0
		 #enqueue的时候是否加锁，当只有一个进程enqueue时可以不加，否则需要加锁
		 sem_wlock = 0
		 #posix信号量名字，在/dev/shm下创建
		 sem_name = /http_server3
		 fifo_path = /tmp/http_server_mq_mcd_2_ccd.fifo
		 wait_sec = 0
		 wait_usec = 10
</root>
